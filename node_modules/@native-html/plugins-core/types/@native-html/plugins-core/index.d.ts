import { LinkPressTarget } from '@formidable-webview/webshell';
import { RenderersProps } from 'react-native-render-html';

/**
 * This function converts an "click event" from webshell to an array of
 * arguments for `onLinkPress` callback from react-native-render-html.
 *
 * @param param0 - The event from webshell
 * @public
 */
export declare function linkPressTargetToOnDOMLinkPressArgs({ uri, classAttribute, downloadAttribute, hrefAttribute, hreflangAttribute, targetAttribute, idAttribute, nameAttribute, referrerpolicyAttribute, relAttribute, typeAttribute }: LinkPressTarget): Parameters<Required<RenderersProps['a']>['onPress']>;

export { }
